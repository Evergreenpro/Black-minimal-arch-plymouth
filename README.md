# Black minimal arch

Based on: HasanAgitUnal's ArchSliderGlowPlymouth
# Installation

Install with these commands:
```bash
git clone https://github.com/Evergreenpro/Black-minimal-arch-plymouth.git
cd Black-minimal-arch-plymouth
sudo ./install.sh install
```

You can remove the theme if you havent deleted the cloned repo folder.
Go to the repo directory and run:
```bash
sudo ./install remove
```

